. Modify the Push to hub step in your workflow file as- name: Pull latest from Hugging Face
  env:
    follows:

```yaml
- name: Pull and push to HF_TOKEN: ${{ secrets.HF_TOKEN }}
  run: |
    git remote hub
  env:
    HF_TOKEN: ${{ secrets.HF_TOKEN }}
  run: |
 add hf https://MayuraKrishna:$HF_TOKEN@huggingface.co/spaces/MayuraKrishna/SearchEngine
    git fetch    git pull https://MayuraKrishna:$HF_TOKEN@huggingface.co/spaces/MayuraKrishna/SearchEngine main --rebase
 hf main
    git merge hf/main || true

-    git push https://MayuraKrishna:$HF_TOKEN@hug name: Push to hub
  env:
    HF_TOKEN: ${{ secrets.HF_TOKEN }}
 gingface.co/spaces/MayuraKrishna/SearchEngine main
